---
title: "A list or custom field selection cannot be matched"
description: "Check the list behind the field, the allowed selection, and the mapping reference type."
outline: [2, 2]
csvGuide: list-and-custom-field-references
lastUpdated: false
head:
  - [meta, { name: robots, content: "index, follow" }]
---

<!-- Generated by scripts/generate-csv-guides.mjs. Edit .vitepress/data/csv-errors instead. -->

[Tools](/tools/) / [CSV Error Translator](/tools/netsuite-csv-error-translator/)

# A list or custom field selection cannot be matched {#list-and-custom-field-references}

Check the list behind the field, the allowed selection, and the mapping reference type.

<CsvGuideContext slug="list-and-custom-field-references" />

## The expense category cannot be matched {#the-expense-category-cannot-be-matched}

**Message looks like**

- `Invalid category reference key xx`

The wording identifies a matching situation, not a confirmed diagnosis for your account.

**What it means**

The expense category cannot be matched.

Import context: Employees and expense categories.

**Check this first**

Check its active state, displayed name, and mapping reference type.

::: details Show the steps

**Check the record reference**

1. Find the field named in the error on the import's Field Mapping page.
2. Open its edit control and check the selected reference type.
3. Compare that setting with the actual CSV value. A number could be a name, an external ID, or an internal ID. Its appearance alone does not establish the type.
4. Locate the intended record in the same account. Confirm its identity and whether it is available for this transaction.
5. Correct the file or mapping so they agree. Use a default only when it applies to every affected row.

If you need to see internal IDs, enable them in your personal preferences or include them in a saved-search export. Supported reference types vary by field. Name matching is generally case-insensitive, so changing capitalization alone should not be the standard recommendation.

[Reference types](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_N349594.html)

For inactive records or subsidiary restrictions, verify the intended business setup before changing it. Those are account decisions, not text-cleanup operations.

:::

<CsvEntryHelp entry-id="EMP-02" />

- [Employee Import Errors](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4568655075.html)

Documentation checked September 16, 2026.

## A custom item field cannot match its selection {#a-custom-item-field-cannot-match-its-selection}

**Message looks like**

- `Invalid custitem reference key xxx`

The wording identifies a matching situation, not a confirmed diagnosis for your account.

**What it means**

The value supplied to a custom item field does not match an allowed selection.

Import context: Item records.

**Check this first**

Identify the field from its script ID, then inspect the list or record it references. Check spelling, spaces, and the chosen reference type. For several selections in one cell, check the multi-select separator too. Confirm a missing selection belongs in the list before adding it.

::: details Show the steps

**Steps for this error**

1. Use the custom field ID in the error to open the field definition and identify its source list or record.
2. Compare the CSV value with an allowed active selection and the chosen reference type.
3. For multi-select values, verify the configured separator before adding any missing list option.

:::

<CsvEntryHelp entry-id="ITM-03" />

- [Invalid Custitem Reference Key xxx](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1534426278.html)

Documentation checked September 16, 2026.

## The lead source cannot be matched {#the-lead-source-cannot-be-matched}

**Message looks like**

- `Invalid Lead Source Reference Key`

The wording identifies a matching situation, not a confirmed diagnosis for your account.

**What it means**

NetSuite cannot match the lead source.

Import context: Leads and prospects.

**Check this first**

Check the configured list and include the campaign identifier when it forms part of the displayed value. The list's location depends on whether Marketing Automation is enabled.

::: details Show the steps

**Check the record reference**

1. Find the field named in the error on the import's Field Mapping page.
2. Open its edit control and check the selected reference type.
3. Compare that setting with the actual CSV value. A number could be a name, an external ID, or an internal ID. Its appearance alone does not establish the type.
4. Locate the intended record in the same account. Confirm its identity and whether it is available for this transaction.
5. Correct the file or mapping so they agree. Use a default only when it applies to every affected row.

If you need to see internal IDs, enable them in your personal preferences or include them in a saved-search export. Supported reference types vary by field. Name matching is generally case-insensitive, so changing capitalization alone should not be the standard recommendation.

[Reference types](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_N349594.html)

For inactive records or subsidiary restrictions, verify the intended business setup before changing it. Those are account decisions, not text-cleanup operations.

:::

<CsvEntryHelp entry-id="REL-01" />

- [General Relationship Import Errors](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4570061540.html)

Documentation checked September 16, 2026.

## A website is needed to identify its category {#a-website-is-needed-to-identify-its-category}

**Message looks like**

- `Invalid category reference key Information for website <NULL>`

The wording identifies a matching situation, not a confirmed diagnosis for your account.

**What it means**

NetSuite needs a website before it can identify the site's category.

Import context: Website categories.

**Check this first**

Map the website or select a default when every row belongs to that site. Then check the category within that site.

::: details Show the steps

**Check the record reference**

1. Find the field named in the error on the import's Field Mapping page.
2. Open its edit control and check the selected reference type.
3. Compare that setting with the actual CSV value. A number could be a name, an external ID, or an internal ID. Its appearance alone does not establish the type.
4. Locate the intended record in the same account. Confirm its identity and whether it is available for this transaction.
5. Correct the file or mapping so they agree. Use a default only when it applies to every affected row.

If you need to see internal IDs, enable them in your personal preferences or include them in a saved-search export. Supported reference types vary by field. Name matching is generally case-insensitive, so changing capitalization alone should not be the standard recommendation.

[Reference types](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_N349594.html)

For inactive records or subsidiary restrictions, verify the intended business setup before changing it. Those are account decisions, not text-cleanup operations.

:::

<CsvEntryHelp entry-id="WEB-01" />

- [Website Import Errors](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4568643764.html)

Documentation checked September 16, 2026.

## Related errors


- [NetSuite cannot identify the record to import](/tools/netsuite-csv-error-translator/record-identifiers)
- [NetSuite says the record already exists](/tools/netsuite-csv-error-translator/duplicate-records)
- [NetSuite cannot match an item in a CSV import](/tools/netsuite-csv-error-translator/item-reference)
- [The item unit or record type does not match](/tools/netsuite-csv-error-translator/item-units-and-types)

<CsvGuideFooter show-navigation />
