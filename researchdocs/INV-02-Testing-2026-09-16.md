# INV-02 record-type mismatch: local investigation and validation

Expanded the existing entry, not a new catalogue ID. No deployment, commit, push,
or NetSuite record operation. The supplied detailed handoff is retained separately
in INV-02-Content-Handoff.md, excluded from the public site.

## Reproduced miss

INV-02 was present in the catalogue and its generated item-units-and-types guide,
with the stable anchor the-item-type-differs-from-the-import-type. Its family rule
recognized only inventoryitem/serializedinventoryitem. Before the correction, the
reported Other Charge Sale/Purchase message (including repeated spaces), the error
code and record type mismatch alias all produced zero results. This was recognition,
not missing content, a filter exclusion or navigation loss. Shared ranking was not
replaced.

## Targeted changes

- Match the distinctive record-load/different-type prefix, error code and supplied
  aliases independently of the two type identifiers. Remove the overly broad literal
  inventoryitem rule that could collide with other error families.
- Parse actual and requested types from their respective named slots in the original
  text, preserving case and order. Known types have readable labels; unfamiliar
  identifiers remain visible without guessed labels. Missing pairs are not invented.
- Show the general target check first. A dynamic item-import suggestion appears only
  when both parsed types are recognized item types, and requires confirming the
  existing item is the intended target. Reversing the pair reverses its roles.
- Keep the reported Sale/Purchase example explicitly labelled as an example. Item
  import steps and the conditional script-loading branch are separate from the
  general explanation. No automatic Items or Inventory Item context is selected.
- Preserve the established guide route and section anchor. Other unit guidance on
  the shared page remains intact. Use the supplied entry title and search snippet.
- Five follow-up branches start without a selected answer. Keep public source links
  and documented/source-reviewed status without claiming an account-tested fix.
- Keep the main specification's existing INV-02 identity and all 118 catalogue IDs.
  There are still 35 guide pages.

## Verification

- Local production build passed; 39 automated tests passed, including source-ID,
  generated header-index and destination-anchor coverage.
- Twelve browser cases passed through both translator and header search and guide
  navigation: exact report, reversed/case variant, documented inventory example,
  synthetic lot/serialized, customer/vendor, unfamiliar pair, prefix only, code only,
  missing Type, units, parent reference and unsupported transformation.
- Browser checks verified actual/requested order and raw identifiers, no item-specific
  recommendation for non-item/unfamiliar pairs, no fabricated missing types, all five
  follow-up branches, blank initial selection and both existing shared-page anchors.
- Existing automated regressions for invalid items, inventory status, received-line
  restriction, ambiguous messages and other catalogue families remain green.
- Original queries remain in memory only; they are not added to URLs or analytics.

## Evidence boundary

Public Oracle error-code, item-import, type-reference and record.load documentation
was opened during implementation. The supplied SuiteAnswers 34807 excerpt is described
as user-supplied, not independently authenticated. Its 05/08/2024 date remains as supplied.
Synthetic parser pairs are not reports of account failures. No actual NetSuite import
was reproduced or corrected. In particular, a script load must be assessed under its
API rules; this guide does not claim every script call requires identical subtype labels.
