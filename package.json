{
  "name": "mysuite-blog",
  "version": "2.0.0",
  "description": "MySuite - NetSuite Consulting Blog",
  "scripts": {
    "generate:csv-guides": "node scripts/generate-csv-guides.mjs",
    "predev": "npm run generate:csv-guides",
    "dev": "vitepress dev",
    "prebuild": "npm run generate:csv-guides",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "test": "node --test tests/*.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pjolson/MySuiteBlog.git"
  },
  "license": "ISC",
  "devDependencies": {
    "vitepress": "^1.6.0"
  },
  "dependencies": {
    "@netlify/blobs": "^11.1.0"
  }
}
